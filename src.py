import numpy as np
import BeautifulSoup as bs

def bayes():
    print("spam")

def mime_header_parser(text):
	# scanning for html
	if "Content-Type" in text:
		

def is_text(lines):
	header = []
	for line in lines:
		if line == "\n":
			return lines
	header.append(line)
		lines = lines[1:]
